<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <!-- Android 11+ package visibility: allow binding to the on-device speech
         recognition service (Google's offline ASR) used by AndroidSpeechRecognizer. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:label="Qdrant Glasses"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Glasses">

        <!-- Android 12+ isolates vendor native libs by namespace: to dlopen the system's
             /vendor/lib64/libcdsprpc.so (FastRPC transport to the Hexagon DSP, needed by the
             QNN HTP delegate) the app must declare it here, else QNN fails with
             "libcdsprpc.so not found in namespace classloader-namespace". required=false so
             the app still runs on devices that lack it. -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
